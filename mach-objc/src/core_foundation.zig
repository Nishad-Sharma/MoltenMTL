pub const TimeInterval = f64;

/// Signed integer used by CoreFoundation APIs for sizes, indices, counts, etc. Matches
/// `CFIndex` (signed long) in `<CoreFoundation/CFBase.h>`.
pub const Index = c_long;
