#version 450

layout(local_size_x = 64) in;

layout(set = 0, binding = 0) buffer DataBuffer {
    uint data[];
};

void main() {
    uint idx = gl_GlobalInvocationID.x;
    data[idx] = data[idx] * 2;
}
